APP_NAME=HomeHub NZ
APP_ENV=development
DEBUG=true
API_V1_PREFIX=/api/v1

# Use host "localhost" when running the API outside Docker.
DATABASE_URL=postgresql://homehub:homehub123@localhost:5432/homehub_db

SECRET_KEY=change-me-in-production
